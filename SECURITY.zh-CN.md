# 安全政策

[English](SECURITY.md)

## 支持的版本

初始开发阶段会为默认分支上最新的 `0.1.x` 源码提供安全修复。

## 私下报告

不要创建包含漏洞利用细节、凭据、私有路径或未脱敏 AssumeZero 产物的公开 Issue。在可用时，请使用 GitHub 针对 `y4ho0/assume-zero` 的私有安全公告报告功能。

请包含：

- AssumeZero 版本和提交；
- 操作系统和架构；
- 能复现问题的最小可信夹具；
- 源文件是否发生变化；
- 受影响的报告字段或输出路径；
- 已脱敏的复现步骤。

不要发送真实 Token 或环境变量值。请替换为明确无效的测试值。

## 范围

安全问题包括：AssumeZero 的复制机制导致源工作区被写入、不安全的符号链接穿越、继承的环境值被持久化、非 Shell 模式中的命令参数注入，以及具有误导性的安全边界行为。

可信的被测命令主动读写当前用户可访问资源，属于已记录的“非沙箱”边界；但相关文档缺失仍欢迎报告。
